import { BooleanIconPipe } from './boolean-icon.pipe';

describe('BooleanIconPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanIconPipe();
    expect(pipe).toBeTruthy();
  });
});
